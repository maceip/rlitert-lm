# Tests

Integration tests for litert-lm.

## MCP Tests (Python)

Located in `mcp-tests/` - Python integration tests using uv.

```bash
cd mcp-tests
uv run test_mcp_client.py
```

See `mcp-tests/README.md` for details.
